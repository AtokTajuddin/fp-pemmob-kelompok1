// Deprecated mock register screen.
// Use the real `RegisterScreen` which integrates with Firebase/local auth.

@Deprecated('Mock register screen removed — use real RegisterScreen')
void mockRegisterScreenDeprecated() {
  throw UnsupportedError(
    'Mock register screen removed. Use real RegisterScreen.',
  );
}
